#!/bin/bash

# PriceWatch AI - Copy & Paste Deployment Commands
# Use this if you want to run commands manually step-by-step

cat << 'EOF'

╔══════════════════════════════════════════════════════════════╗
║                                                              ║
║            PRICEWATCH AI - DEPLOYMENT COMMANDS               ║
║                                                              ║
║            Copy and paste these commands in order            ║
║                                                              ║
╚══════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────────────────────────┐
│ STEP 1: AUTHENTICATE WITH GITHUB                             │
└──────────────────────────────────────────────────────────────┘

Run this command and follow the prompts:

    gh auth login


┌──────────────────────────────────────────────────────────────┐
│ STEP 2: CREATE GITHUB REPOSITORY & PUSH CODE                │
└──────────────────────────────────────────────────────────────┘

Option A - Public Repository (Recommended for portfolio):

    cd /data/molt/pricewatch-ai
    gh repo create pricewatch-ai --public --source=. --remote=origin --push


Option B - Private Repository:

    cd /data/molt/pricewatch-ai
    gh repo create pricewatch-ai --private --source=. --remote=origin --push


┌──────────────────────────────────────────────────────────────┐
│ STEP 3: INSTALL RAILWAY CLI                                  │
└──────────────────────────────────────────────────────────────┘

    sudo npm install -g @railway/cli


┌──────────────────────────────────────────────────────────────┐
│ STEP 4: LOGIN TO RAILWAY                                     │
└──────────────────────────────────────────────────────────────┘

This will open your browser for authentication:

    railway login


┌──────────────────────────────────────────────────────────────┐
│ STEP 5: INITIALIZE RAILWAY PROJECT                           │
└──────────────────────────────────────────────────────────────┘

    cd /data/molt/pricewatch-ai
    railway init


┌──────────────────────────────────────────────────────────────┐
│ STEP 6: ADD DATABASES                                        │
└──────────────────────────────────────────────────────────────┘

Add PostgreSQL:

    railway add --database postgres


Add Redis:

    railway add --database redis


┌──────────────────────────────────────────────────────────────┐
│ STEP 7: SET ENVIRONMENT VARIABLES                            │
└──────────────────────────────────────────────────────────────┘

Generate and set secret key:

    railway variables set SECRET_KEY=$(openssl rand -hex 32)


Set Stripe key (replace with your actual key):

    railway variables set STRIPE_SECRET_KEY=sk_test_YOUR_ACTUAL_KEY_HERE


Set SendGrid key (replace with your actual key):

    railway variables set SENDGRID_API_KEY=SG.YOUR_ACTUAL_KEY_HERE


Set sender email:

    railway variables set SENDGRID_FROM_EMAIL=noreply@yourdomain.com


┌──────────────────────────────────────────────────────────────┐
│ STEP 8: DEPLOY TO RAILWAY                                    │
└──────────────────────────────────────────────────────────────┘

    railway up


┌──────────────────────────────────────────────────────────────┐
│ STEP 9: GET YOUR DEPLOYMENT URL                              │
└──────────────────────────────────────────────────────────────┘

    railway domain


┌──────────────────────────────────────────────────────────────┐
│ STEP 10: VIEW LOGS (Optional)                                │
└──────────────────────────────────────────────────────────────┘

Watch logs in real-time:

    railway logs --tail


┌──────────────────────────────────────────────────────────────┐
│ STEP 11: TEST YOUR DEPLOYMENT                                │
└──────────────────────────────────────────────────────────────┘

Replace YOUR_URL with your actual Railway URL:

    curl https://YOUR_URL.railway.app/health


Expected response: {"status": "healthy"}


╔══════════════════════════════════════════════════════════════╗
║                                                              ║
║                    🎉 DEPLOYMENT COMPLETE!                   ║
║                                                              ║
║  Your app should now be live at your Railway URL            ║
║                                                              ║
╚══════════════════════════════════════════════════════════════╝


═══════════════════════════════════════════════════════════════
ALTERNATIVE: DEPLOY VIA RAILWAY WEB UI (NO CLI NEEDED)
═══════════════════════════════════════════════════════════════

If you prefer to use the web interface:

1. First, push code to GitHub:

   gh auth login
   cd /data/molt/pricewatch-ai
   gh repo create pricewatch-ai --public --source=. --remote=origin --push

2. Then go to: https://railway.app

3. Click "Login with GitHub"

4. Click "New Project"

5. Choose "Deploy from GitHub repo"

6. Select "pricewatch-ai"

7. Click "+ New" → "Database" → "Add PostgreSQL"

8. Click "+ New" → "Database" → "Add Redis"

9. Go to your service → "Variables" tab and add:
   - SECRET_KEY (generate with: openssl rand -hex 32)
   - STRIPE_SECRET_KEY
   - SENDGRID_API_KEY
   - SENDGRID_FROM_EMAIL

10. Railway will auto-deploy!


═══════════════════════════════════════════════════════════════
GET YOUR API KEYS
═══════════════════════════════════════════════════════════════

Stripe (for payments):
  Sign up: https://stripe.com
  Get key: Dashboard → Developers → API Keys
  Use: sk_test_... (for testing)

SendGrid (for emails):
  Sign up: https://sendgrid.com
  Get key: Settings → API Keys → Create API Key
  Use: SG.... (starts with SG.)
  IMPORTANT: Verify your sender email in SendGrid dashboard


═══════════════════════════════════════════════════════════════
TROUBLESHOOTING
═══════════════════════════════════════════════════════════════

Build failed?
  → Check Railway logs for errors
  → Ensure requirements.txt is valid

Database connection failed?
  → Check that DATABASE_URL is set
  → Ensure PostgreSQL was added in Railway

Celery not starting?
  → Check that REDIS_URL is set
  → Ensure Redis was added in Railway

Need help?
  → Railway docs: https://docs.railway.app
  → Check logs: railway logs


═══════════════════════════════════════════════════════════════
ESTIMATED TIME: 10-15 MINUTES TOTAL
═══════════════════════════════════════════════════════════════

✅ GitHub setup: 2 minutes
✅ Railway deployment: 5 minutes
✅ Environment variables: 3 minutes
✅ Testing: 2 minutes

TOTAL: ~12 minutes to production! 🚀


═══════════════════════════════════════════════════════════════

Good luck! 🎉

EOF
